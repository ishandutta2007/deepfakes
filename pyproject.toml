[tool.poetry]
name = "nextgen-media-platform"
version = "0.1.0"
description = "A platform for State-of-the-Art Diffusion Models and 3D-Aware Methodologies with an Electron UI."
authors = ["AIAgent <agent@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
